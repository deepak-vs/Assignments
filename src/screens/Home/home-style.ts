import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 335,
    left: 30,
    marginBottom: 120,
    justifyContent: 'space-evenly',
  },
  subContainer:{
    marginBottom:100,
  }
})
