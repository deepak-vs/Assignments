import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 335,
    left: 30,
    gap:20,
    paddingBottom:10,
    marginBottom: 150,
    justifyContent: 'space-evenly',
    borderWidth:1,
  },
  subContainer: {
    marginBottom: 100,
  },
})
